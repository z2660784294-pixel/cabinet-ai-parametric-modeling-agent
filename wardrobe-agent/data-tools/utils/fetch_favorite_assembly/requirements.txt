# fetch_favorite_assembly：仅本目录脚本需要的第三方包（crypt_zstd、fetch_assembly）。
# 在本目录执行:
#   pip install -r requirements.txt
# 若需安装整个 data-tools 工具链（Flask UI、requests 等），请改用:
#   pip install -r ../../requirements.txt

zstandard>=0.22.0
